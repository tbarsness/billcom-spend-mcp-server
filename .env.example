# BILL Spend & Expense API token. Generate in BILL → admin user → API tokens.
# In production, the user must have the ADMIN role to generate the token.
BILLCOM_SPEND_API_TOKEN=

# "production" or "sandbox". Defaults to sandbox if unset.
BILLCOM_ENVIRONMENT=sandbox
